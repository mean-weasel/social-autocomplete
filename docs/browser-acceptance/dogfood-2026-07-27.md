# Dogfood acceptance — 2026-07-27

All observations were captured fresh in `en-US`, region `US`, timezone
`America/Phoenix`. No credentials, cookies, account identifiers, private
creative assets, raw DOM, or screenshots are stored.

## Passing live cells

- Codex in-app Browser, YouTube public `search-term`: exact prefix
  `remote work`; captured the first ten visible suggestions, including
  `remote work setup`, `remote work from home`, `remote work tip`, and
  `remote work jobs`. `remote work tip` was inspected in current results.
  After skipping a sponsored card, at least three visible organic titles were
  directly relevant to remote-work tips.
- Codex in-app Browser, YouTube public `hashtag`: exact prefix
  `#remotework`; the native options included exact `#remotework` and
  `#remotejob`, while non-hashtag phrases were excluded. The selected exact
  `#remotework` was searched, and three bounded, distinct, non-sponsored
  results were directly relevant to remote work.
- Codex in-app Browser, Pinterest public `search-term`: exact prefix
  `remote office`; captured ten visible suggestions, including
  `remote office setup`, `remote office ideas`, and
  `remote office desk setup`. `remote office ideas` opened a native Pin result
  surface with bounded visible results.
- Codex Chrome, LinkedIn authenticated `search-term`: exact prefix
  `remote work`; native phrase candidates included `remote work companies`,
  `remote work future`, `remote work compensation`, `remote work attrition`,
  and `remote work loneliness`. Typed company/product entities were excluded.
  `remote work companies` opened native search results with visible post cards.
- Codex Chrome, X authenticated `search-term`: exact prefix `remote work`.
  `remote worker` was the one native phrase candidate in the bounded sample;
  account suggestions were excluded. It opened the native Top/Latest/People/
  Media/Lists result surface with visible post cards.

Displayed autocomplete order was captured only as surface evidence and was not
treated as popularity or performance proof.

## Interrupted, failed, and blocked cells

- TikTok public search loaded a visible Search input, but bounded input
  interaction failed and result landmarks were unavailable. Recorded
  `ui_change`, not native empty.
- Facebook and Instagram authenticated research require the user to sign in;
  no research interaction was attempted.
- Claude clean package validation and `--plugin-dir` loading pass. Live Claude
  in Chrome was unavailable in this Codex-run acceptance environment and is
  recorded as `capability_unavailable`.
- Pinterest hashtag remains explicitly not applicable.

## Defects

- TikTok public search interaction needs a refreshed semantic checkpoint or an
  assisted live mapping pass. This does not affect the explicit-failure path.

No publishing, composer interaction, scraping, private endpoints, challenge
bypass, or unbounded pagination occurred.
